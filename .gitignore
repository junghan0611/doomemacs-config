# System files
.DS_Store
.idea
*.log
tmp/
deprecated
.efrit

__pycache__

# Personal configuration (machine-specific, not shared via git)
custom.el
flycheck_config.el
local/*
per-machine.el
user-keys.el
+office.el

# Private files (synced via Syncthing, not committed to git)
# These files contain personal data, templates, and local content
# Anyone can manage similar private local files alongside their public config
.agent-shell/
.claude/
templates/
*.dat
ccmenu

# Generated/cache files
bin/.last-export/

# Personal dictionaries and abbreviations (machine-specific)
# var/abbrev_defs
# var/aspell.en.pws
# var/hunspell_ko_personal
# var/logo.png

# Security - DO NOT COMMIT
# Sensitive information that should never be in version control
# var/sensitive-strings.txt

# Local-only AI proxy config (ToS-sensitive, never publish)
lisp/ai-gptel-local-proxy.el

# QTermWidget build (external project)
qtermwidget/

docs/plans

*.db
